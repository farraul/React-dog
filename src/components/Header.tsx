const Header = () => {
  return (
    <header className="text-primary bg-primary sticky top-0 text-2xl z-50">
      <div className="mx-auto container flex justify-between p-6">
        <div>Dog project</div>
      </div>
    </header>
  );
};

export default Header;
